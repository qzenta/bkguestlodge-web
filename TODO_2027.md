# Student Accommodation — January 2027 Prep

Tracking doc for Sprint 13 of the Post-Launch Optimisation Sprint brief.
Guest Lodge is the current commercial priority; this page stays live and
indexable but off the main nav until closer to launch (see
`components/layout/Nav.tsx` and the homepage secondary banner in
`app/page.tsx`, which is its main discovery path today).

## Current state

- `app/accommodation/student-accommodation/page.tsx` exists and is indexed
  (`app/sitemap.ts`), but is not linked from primary nav.
- Homepage carries a subordinate "Also opening January 2027" banner linking
  to the page.

## Still needed before/at launch (per Sprint 13)

- [ ] Rework the page into a full "Coming January 2027" landing page:
      overview, benefits, room types, security, facilities, FAQ
- [ ] Add a waiting-list form (capture + storage/email routing TBD)
- [ ] Dedicated SEO metadata for the page
- [ ] Re-add to main nav and promote from the homepage banner once
      launch is imminent — this should be a config/content flip, not a
      rebuild
- [ ] Decide sitemap priority/visibility ramp as launch date approaches
